# WT

## Link ของอาจารย์
https://tinnakorn.cs.rmu.ac.th/courses

**ที่อาจารย์ให้ใช้งาน และสำหรับดูสี**
- https://tinnakorn.cs.rmu.ac.th/courses/technologyweb/css/graphic.css => CSS CDN ของอาจารย์
- https://tinnakorn.cs.rmu.ac.th/courses/technologyweb/css/rings.css => CSS CDN ของอาจารย์
- https://codepen.io/spark => เป็น URL สำหรับเขียนโค้ดแข่งขัน JavaScript, CSS
- https://www.w3schools.com/Colors/ => Web สำหรับสอนเกือบทุกภาษา
- https://www.pantone.com/ => วิจัยสีที่ควรใช้ออกแบบในแต่ละปี
- https://coolors.co/ => Web สำหรับดูกลุ่มของสีสำหรับไปใช้งานบน WebSite
- https://picsum.photos/200/300?random=1 => Random Image
- https://caniuse.com/ => สำหรับตรวจสอบคำสั่งว่าคำสั่งที่เราตรวจสอบรองรับกับ Browser ไหนได้บ้างและใช้กับ Browser ไหนได้บ้าง
- https://i.pravatar.cc/150?img=4 => Random รูป Avatar

---

## วิธีการใช้งานสี
1. Color Name => คือการเรียกใช้สีด้วยชื่อสีที่เป็นภาษาอังกฤษ(Key ลัดในการดึงข้อมูลชื่อของสีบน CSS คือ [ctrl] + [space bar]) ตัวอย่าง : color: red;
2. function RGB(Red, Green, Blue), CRT(ฟิสิกส์), pixel(คือจุดบนหน้าจอ) => เป็นการเรียกผ่าน Function RGB ที่เป็นแม่สี ตัวอย่าง RGB : color: rgb(255, 255, 0); ตัวอย่าง RGBA : color: rgba(red, green, blue, alpha); ค่าสีมาตรฐานเริ่มจาก 0 - 255 ใช้ข้อมูล 8 bit ในการเก็บ
3. hexa decimal => ตัวอย่าง(สีเหลือง) : color: #fff000; เป็นเลขฐาน 16

---

## Basic Layout
- display = การแสดงผล inline-flex(บรรทัดนั้นๆ), grid()
- grid-template-columns = เป็นการกำหนด column ใช้ได้ต่อเมื่อ display เป็น grid ตัวอย่าง 4 column: grid-template-columns: 1fr 1fr 1fr 1fr; หรือ grid-template-columns: repeat(4, 1fr); ตัวเลขเช่น 1fr คือการจัดการพื้นที่
- gap = ช่องว่าง ใช้ได้ต่อเมื่อใช้ display เป็น flex, grid
- flex-direction = จัดทิศทาง default คือ row ถ้าใช้เป็น column คือการจัดแนวตั้ง reverse(กลับด้านใช้บน)
- justify-content = จัดทิศทางแนวนอน space-between(การคำนวนพื้นที่เองยึดตามขอบจอ), space-between(คำนวนเฉลี่ยทั้งจอ)
- flex-wrap = ปรับให้ขึ้นหน้าจอใหม่
- row = แนวนอน
- column = แนวตั้ง
- position = ตำแหน่ง วัตถุ default เรียงจากซ้ายไปขวา จากบนลงล่าง, relative(), fixed(กำหนดตำแหน่งตายตัว จะยึด Screen หน้าจอเป็นหลัก), absolute()
- linear-gradient = ตัวอย่าง: background-image: linear-gradient(to right, green, red); ตัวอย่างไล่เฉดสีเอียงแบบ 135 องศา จากเขียวไปแดง: background-image: linear-gradient(135deg, green, red);
- z-index = default คือ 1 ลำดับการจัดวัตถุ
- overflow = hidden(สำหรับกำหนดให้ถ้ามันล้นออกมาให้มันหายไป), scroll(สำหรับให้ Scroll Mouse ดูในส่วนที่เกินมาได้) ตัวอย่าง : overflow: hidden;
- padding = กำหนดระยะห่างภายในวัตถุ ตัวอย่าง : padding: 1px; ก็คือวัตถุภายในจะห่างจากขอบทุกมุม 1px
- color = จัดการสี transparent(ทำให้ตัวอักษรหายไป)
- background-clip = text(เป็นการนำ background การเป็นรูปภาพให้อยู่ในตัวหนังสือ)
- filter = การทำเงา ตัวอย่าง: filter: drop-shadow(5px 5px 8px gray);
- @keyframes {ชื่อ Keyframe} = การทำ animation

ตัวอย่าง Keyframes
```
.text-ct {
    font-size: 200px;
    font-weight: bold;
    color: transparent;
    background-image: url("https://picsum.photos/800/300?random=1");
    background-clip: text;
    filter: drop-shadow(5px 5px 8px gray);
    animation: movement 5s infinite linear;
    &:hover {
        animation-play-state: paused;
    }
}

@keyframes movement {
    0% {
        /* transform: scale(1) translate(0, 0); */
        transform: rotate(0)
    }
    
    50% {
        /* transform: scale(1.3) translate(0, -120px); */
        transform: rotate(360deg);
    }
}
```
